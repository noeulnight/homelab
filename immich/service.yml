apiVersion: v1
kind: Service
metadata:
  name: immich-server-service
spec:
  selector:
    app: immich-server
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: immich-ml-service
spec:
  selector:
    app: immich-ml
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
---
apiVersion: v1
kind: Service
metadata:
  name: immich-web-service
spec:
  selector:
    app: immich-web
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: immich-postgres-service
spec:
  selector:
    app: immich-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
