apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overleaf-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: overleaf-pv
  resources:
    requests:
      storage: 20Gi
