apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slash-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: slash-pv
  resources:
    requests:
      storage: 5Gi
