apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd-image-updater.argoproj.io/image-list: coder=coder/server:latest
    argocd-image-updater.argoproj.io/coder.update-strategy: digest
  name: coder
  namespace: argocd
spec:
  destination:
    namespace: coder
    server: https://kubernetes.default.svc
  ignoreDifferences:
    - jsonPointers:
        - /data
      kind: Secret
  project: default
  sources:
    - chart: coder
      repoURL: https://helm.coder.com/v2
      targetRevision: "*"
      helm:
        releaseName: coder
        valuesObject:
          coder:
            env:
              - name: CODER_PG_CONNECTION_URL
                valueFrom:
                  secretKeyRef:
                    name: coder-secret
                    key: CODER_PG_CONNECTION_URL
              - name: CODER_ACCESS_URL
                value: https://coder.limtaehyun.dev
              - name: CODER_WILDCARD_ACCESS_URL
                value: "*.coder.limtaehyun.dev"
              - name: CODER_OIDC_ICON_URL
                value: https://auth.limtaehyun.dev/static/dist/assets/icons/icon.png
              - name: CODER_OIDC_ISSUER_URL
                value: https://auth.limtaehyun.dev/application/o/coder/
              - name: CODER_OIDC_EMAIL_DOMAIN
                value: limtaehyun.dev,naver.com
              - name: CODER_OIDC_SIGN_IN_TEXT
                value: Sign in with HomeLab
              - name: CODER_OIDC_CLIENT_ID
                valueFrom:
                  secretKeyRef:
                    name: coder-secret
                    key: CODER_OIDC_CLIENT_ID
              - name: CODER_OIDC_CLIENT_SECRET
                valueFrom:
                  secretKeyRef:
                    name: coder-secret
                    key: CODER_OIDC_CLIENT_SECRET
            serviceAccount:
              workspacePerms: true
              enableDeployments: true
              workspaceNamespaces:
                - name: coder-workspace
            service:
              enable: true
              type: ClusterIP
            ingress:
              enable: true
              className: nginx
              host: coder.limtaehyun.dev
    - path: coder
      repoURL: git@github.com:noeulnight/homelab.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - RespectIgnoreDifferences=true
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
      - ServerSideApply=true
      - PruneLast=true
