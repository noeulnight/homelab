apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: health-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: health-pv
  resources:
    requests:
      storage: 5Gi
