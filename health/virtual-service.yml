apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: health
spec:
  hosts:
    - "health.lth.so"
  gateways:
    - istio-ingress/public-gateway
  http:
    - route:
        - destination:
            host: health-service.health.svc.cluster.local
            port:
              number: 8080
      headers:
        response:
          set:
            Strict-Transport-Security: max-age=31536000; includeSubDomains
