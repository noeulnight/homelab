apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booklore-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: booklore-pv
  resources:
    requests:
      storage: 5Gi
