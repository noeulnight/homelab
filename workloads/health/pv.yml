apiVersion: v1
kind: PersistentVolume
metadata:
  name: health-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/data/health
    type: DirectoryOrCreate
