apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: technitium-pv
  resources:
    requests:
      storage: 5Gi
