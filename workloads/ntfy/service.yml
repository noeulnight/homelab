apiVersion: v1
kind: Service
metadata:
  name: ntfy-service
  labels:
    app.kubernetes.io/name: ntfy-service
spec:
  selector:
    app: ntfy
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
