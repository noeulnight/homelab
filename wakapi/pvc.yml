apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wakapi-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: wakapi-pv
  resources:
    requests:
      storage: 5Gi
