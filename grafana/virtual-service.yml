apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: grafana
spec:
  hosts:
    - "monitoring.lth.so"
    - "monitoring.limtaehyun.dev"
  gateways:
    - istio-ingress/public-gateway
  http:
    - match:
        - headers:
            :authority:
              exact: monitoring.limtaehyun.dev
      redirect:
        authority: monitoring.lth.so
    - route:
        - destination:
            host: grafana.grafana.svc.cluster.local
            port:
              number: 80
      headers:
        response:
          set:
            Strict-Transport-Security: max-age=31536000; includeSubDomains
