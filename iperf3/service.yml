apiVersion: v1
kind: Service
metadata:
  name: iperf3
spec:
  selector:
    app: iperf3
  ports:
    - name: iperf
      protocol: TCP
      port: 5201
      targetPort: 5201
    - name: iperf-udp
      protocol: UDP
      port: 5201
      targetPort: 5201
