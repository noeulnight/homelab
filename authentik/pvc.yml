apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  volumeName: authentik-pv
  resources:
    requests:
      storage: 10Gi
